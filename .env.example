# Supabase (server-side). The app ships with working defaults baked in for this
# test project, so these are optional overrides.
SUPABASE_URL=https://efztaxrhinpfbxvwxrlq.supabase.co
SUPABASE_ANON_KEY=sb_publishable_oOOA9HweZyqQX-eFXXDFvQ_eOTjDvn9

# ---------------------------------------------------------------------------
# Connect an AI provider later. With no key set, the dashboard runs on the
# built-in "sample" provider so the whole flow is usable as a framework.
# To use Claude: set ANTHROPIC_API_KEY here and switch the provider to
# "anthropic" on the Knowledge & Training page in the dashboard.
# ---------------------------------------------------------------------------
ANTHROPIC_API_KEY=
